const home = require('./home')
const login = require('./login')
const tracking = require('./tracking')
const trackingw = require('./trackingw')

const general = {
    home,
    tracking,
    trackingw,
    login
  }

module.exports = general;

